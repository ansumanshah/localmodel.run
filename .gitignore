# build output
dist/
.astro/

# deps
node_modules/

# env / secrets
.env
.env.*
!.env.example

# private agent/research notes + installed skills — never commit (not OSS source)
.research/
.claude/
.agents/

# design-sync workspace (local artifact for claude.ai/design; not site source)
# holds config.json + NOTES.md (control state) and bundle/ (the uploaded artifact)
.design-sync/

# os / editor
.DS_Store
*.log
.vscode/
.idea/
